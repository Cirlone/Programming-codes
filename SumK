#include <iostream>
using namespace std;
int k;
int sub(int n, int v[], int k)
{  int s=0;
    for(int i=0;i<n;i++)
        if(v[i]%2!=0&&k>0)
          { s=s+v[i];
             k--;
          
          }
 if(k!=0)
      return -1;
 return s;
}
